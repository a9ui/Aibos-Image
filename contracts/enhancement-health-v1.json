{
  "schemaVersion": 1,
  "contractId": "PV-ENHANCE-HEALTH-001",
  "protocol": "aibos.enhancement-health/v1",
  "route": {
    "method": "GET",
    "path": "/api/enhance/health",
    "cacheControl": "no-store",
    "loopbackOnly": true
  },
  "passiveRead": {
    "createsJobs": false,
    "startsWorker": false,
    "wakesQueue": false,
    "claimsJobs": false,
    "retriesJobs": false,
    "pollsComfyUi": false
  },
  "status": [
    "healthy",
    "working",
    "needs-attention"
  ],
  "issues": [
    "multiple-running-jobs",
    "running-without-worker-identity",
    "running-without-local-pump",
    "queued-without-pump",
    "worker-loop-failing",
    "non-loopback-server",
    "non-loopback-comfyui"
  ],
  "requiredFields": [
    "version",
    "generatedAt",
    "status",
    "issues",
    "runtime.sourceRevision",
    "runtime.sourceDirty",
    "runtime.buildId",
    "runtime.serverHost",
    "runtime.serverPort",
    "runtime.serverStartedAtUtc",
    "runtime.processId",
    "store.version",
    "jobs.counts",
    "jobs.current",
    "jobs.lastClaimAt",
    "jobs.lastProgressAt",
    "jobs.lastTerminalAt",
    "worker.pumpRunning",
    "worker.processWorkerInstanceId",
    "worker.consecutiveWorkerFailures",
    "worker.lastWorkerError",
    "worker.lastWorkerErrorAt",
    "worker.lastLeaseContentionAt",
    "comfyUi.endpoint",
    "comfyUi.loopback",
    "comfyUi.promptId",
    "comfyUi.submissionState"
  ],
  "readerRules": {
    "missingRoute": "show health unavailable without blocking the existing jobs response",
    "invalidPayload": "show health unavailable without mutating jobs or starting the companion",
    "unknownFields": "ignore",
    "unknownStatus": "treat the health payload as unavailable",
    "unknownIssue": "preserve the needs-attention status and show a generic attention message",
    "missingWorkerPaused": "keep health and jobs readable but disable pause and resume controls",
    "missingQueuedPhotorealPromptUpdate": "keep health and jobs readable but hide the queued prompt-update action",
    "missingTerminalHistoryBatchDismissV1": "keep health and jobs readable and fall back to protected per-job terminal history dismissal",
    "missingQueuedJobsBatchCancelV1": "keep health and jobs readable and fall back to protected per-job queued cancellation",
    "missingQueuedJobsBatchReorderV1": "keep health and jobs readable and fall back to protected per-job queued reordering without sending a final-order body to an older route",
    "missingTerminalHistoryTargetsV1": "keep health and jobs readable and never redefine all as only the loaded history window",
    "missingTerminalHistoryBatchRetryV1": "keep health and jobs readable but never retry only the loaded terminal history window as if it were all",
    "terminalHistoryTargetsV1Dependency": "use authoritative targets only with the matching compact mutation capability: dismiss requires terminalHistoryBatchDismissV1 and retry requires terminalHistoryBatchRetryV1, so a plan never degrades into thousands of per-job requests",
    "missingPhotorealPromptControlsV2": "keep health and jobs readable but block new photoreal submissions so an older companion cannot silently ignore Negative, LoRA OFF, or the removal of legacy hidden prompt text",
    "kreaAnimeToRealV1": "a boolean that means the health writer and reader recognize the exact comfyui-krea2-anime-to-real-edit-v1-photoreal adapter row; it does not assert that its assets, backend, workflow, or runtime are installed, verified, ready, or enabled",
    "missingKreaAnimeToRealV1": "keep health and jobs readable but block publication and retry for the exact comfyui-krea2-anime-to-real-edit-v1-photoreal adapter; missing, false, malformed, or legacy capability spellings fail closed",
    "duplicateKreaAnimeToRealV1": "treat duplicate capabilities objects or duplicate kreaAnimeToRealV1 members as malformed even when one occurrence is boolean true; disable the selector and block publication and retry",
    "deferredBackendSkipV1": "an exactly true boolean that permits execution selection to skip a recognized queued row whose backend is temporarily unavailable while leaving that durable row and its presentation order unchanged under PV-ENHANCE-QUEUE-001",
    "backendAvailabilityKreaPhotorealV1": "an optional bounded passive projection at backendAvailability.kreaPhotorealV1; it reports only recognized adapter ids, booleans, fixed reason text, and queued or waiting counts, never paths, secrets, active probing, process starts, or queue mutations",
    "missingDeferredBackendSkipV1": "keep health and jobs readable and do not infer unavailable-backend skip behavior; readers never wake or reorder the queue from health",
    "missingAtomicImageEnqueueNext": "keep health and jobs readable but hide or block enqueue-next actions so an older companion cannot silently append the job to the tail",
    "missingDisplayedManagedVideoSourceV1": "keep health and jobs readable but block video enqueue from a displayed managed still so an older companion cannot reinterpret it as an Original source",
    "missingStoreBackendGeneration": "treat the store as a compatible legacy backend and keep the existing jobs API behavior",
    "missingStoreRevisions": "keep health and jobs readable; readers must not invent revision values",
    "inventoryRevision": "an optional monotonic commit revision; it may advance for progress or heartbeat writes and is not by itself a signal to reload the full jobs inventory",
    "catalogRevision": "an optional monotonic revision for WPF-visible managed-media changes; it stays stable for heartbeat and progress-only writes",
    "queueOrderRevision": "an optional non-negative monotonic safe integer in store.queueOrderRevision; a change invalidates a cached queued ordering even when status counts are unchanged",
    "pausedDisplay": "show Paused as an intentional state; if one job is running, explain that it may finish while no next job starts",
    "polling": "request health only while the Jobs workspace performs its existing passive refresh",
    "timeoutPolicy": "do not infer a stall from elapsed time alone"
  }
}
